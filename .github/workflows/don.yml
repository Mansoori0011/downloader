name: دانلود فیلم با انتخاب کیفیت

on:
  workflow_dispatch:
    inputs:
      video_url:
        description: 'لینک صفحه فیلم (view_video.php?viewkey=...)'
        required: true
        type: string
      quality:
        description: 'کیفیت مورد نظر'
        required: true
        type: choice
        default: 'best'
        options:
          - 'best'
          - '1080p'
          - '720p'
          - '480p'
          - '360p'

jobs:
  download:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: master

      - name: نصب yt-dlp
        run: |
          pip install yt-dlp

      - name: دانلود فیلم با کیفیت انتخاب شده
        run: |
          URL="${{ github.event.inputs.video_url }}"
          QUALITY="${{ github.event.inputs.quality }}"
          TIMESTAMP=$(date +'%Y%m%d_%H%M%S')
          
          echo "═══════════════════════════════════════════════════════════"
          echo "🎬 در حال دانلود فیلم"
          echo "🔗 لینک: $URL"
          echo "🎚️ کیفیت: $QUALITY"
          echo "═══════════════════════════════════════════════════════════"
          
          # انتخاب کیفیت
          case "$QUALITY" in
            "best")
              yt-dlp -f best -o "video_${TIMESTAMP}.mp4" "$URL"
              ;;
            "1080p")
              yt-dlp -f "best[height<=1080]" -o "video_${TIMESTAMP}.mp4" "$URL"
              ;;
            "720p")
              yt-dlp -f "best[height<=720]" -o "video_${TIMESTAMP}.mp4" "$URL"
              ;;
            "480p")
              yt-dlp -f "best[height<=480]" -o "video_${TIMESTAMP}.mp4" "$URL"
              ;;
            "360p")
              yt-dlp -f "best[height<=360]" -o "video_${TIMESTAMP}.mp4" "$URL"
              ;;
          esac
          
          echo "✅ دانلود کامل شد: video_${TIMESTAMP}.mp4"
          echo "FILE_NAME=video_${TIMESTAMP}.mp4" >> $GITHUB_ENV

      - name: ذخیره در مخزن
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add "${{ env.FILE_NAME }}"
          git commit -m "دانلود فیلم با کیفیت ${{ github.event.inputs.quality }}" || echo "تغییری وجود ندارد"
          git push origin master

      - name: آپلود Artifact
        uses: actions/upload-artifact@v4
        with:
          name: downloaded-video
          path: ${{ env.FILE_NAME }}